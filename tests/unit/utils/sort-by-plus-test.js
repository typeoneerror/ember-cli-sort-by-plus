import sortByPlus from 'dummy/utils/sort-by-plus';
import { module, test } from 'qunit';

module('Unit | Utility | sort by plus');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortByPlus();
  assert.ok(result);
});
